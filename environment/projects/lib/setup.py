### pip install -e ~projects/lib #######################################

from setuptools import setup

setup(name='pylocal',
      version='0.0',
      description='Local Python utilities',
      packages=['pylocal'])
