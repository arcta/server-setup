#!/bin/bash

########################################################################
### start all projects service
########################################################################
source ~/.local.cnf

LIST=$(ls ~/projects)
for project in ${LIST[*]}
do
    if [ -d "${HOME}/projects/${project}" ]; then
        if [ -d "${HOME}/projects/${project}/node-app" ]; then
            . ~/projects/bin/start-app $project
        fi

        if [ -d "${HOME}/projects/${project}/python-api" ]; then
            . ~/projects/bin/start-api $project
        fi
    fi
done
echo "Done"
