#!/bin/bash

########################################################################
### DEV ONLY: upgrade all to the latest available version
########################################################################
pip install -U pip
pip freeze --local | grep -v '^\-e' | cut -d = -f 1  | xargs -n1 pip install -U
